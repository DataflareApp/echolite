[package]
name = "echolite"
edition = "2024"
version = "0.2.5"
publish = false

[workspace]
members = ["client", "protocol"]

[profile.release]
lto = true
codegen-units = 1
strip = "debuginfo"

[profile.dev.package.argon2]
opt-level = 3

[features]
default = ["rusqlite/bundled"]

[dependencies]
zeroize = { version = "1.9.0", features = ["derive"] }
tracing = "0.1.44"
tracing-subscriber = "0.3.23"
thiserror = "2.0.20"
protocol = { path = "./protocol" }
clap = { version = "4.6.6", features = ["derive", "env"] }
rusqlite = { version = "0.40.2", features = ["column_decltype"] }
tokio = { version = "1.53.1", features = [
    "macros",
    "rt-multi-thread",
    "net",
    "sync",
    "time",
] }
