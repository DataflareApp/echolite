[package]
name = "echolite"
edition = "2024"
publish = false

[workspace]
members = ["client", "protocol"]

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"

[dependencies]
tracing = "0.1.41"
tracing-subscriber = "0.3.19"
thiserror = "2.0.12"
protocol = { path = "./protocol" }
clap = { version = "4.5.40", features = ["derive", "env"] }
rusqlite = { version = "0.36.0", features = ["column_decltype", "bundled"] }
tokio = { version = "1.45.1", features = [
    "macros",
    "rt-multi-thread",
    "net",
    "sync",
    "time",
] }
rand = "0.9.1"
