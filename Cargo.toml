[package]
name = "echolite"
edition = "2024"
version = "0.2.4"
publish = false

[workspace]
members = ["client", "protocol"]

[profile.release]
lto = true
codegen-units = 1
strip = "debuginfo"

[profile.dev.package.argon2]
opt-level = 3

[features]
default = ["rusqlite/bundled"]

[dependencies]
zeroize = { version = "1.8.2", features = ["derive"] }
tracing = "0.1.44"
tracing-subscriber = "0.3.23"
thiserror = "2.0.18"
protocol = { path = "./protocol" }
clap = { version = "4.6.1", features = ["derive", "env"] }
rusqlite = { version = "0.40.0", features = ["column_decltype"] }
tokio = { version = "1.52.3", features = [
    "macros",
    "rt-multi-thread",
    "net",
    "sync",
    "time",
] }
